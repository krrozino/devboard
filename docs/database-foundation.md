# Database foundation

The initial PostgreSQL schema is defined in `src/db/schema/core.ts` and managed with Drizzle.

The first generated migration is created in the `chore/database-foundation` branch by the `Generate Database Foundation` workflow, then reviewed before merging into `main`.
